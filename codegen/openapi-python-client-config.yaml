# Configuration for openapi-python-client code generation
# This enables enumeration of duplicate model names instead of skipping them
# Required for complete HAProxy Dataplane API v3 client generation
enumerate_duplicate_model_names: true
