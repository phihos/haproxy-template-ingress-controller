{{- if .Values.rbac.create }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "haproxy-template-ic.fullname" . }}
  labels:
    {{- include "haproxy-template-ic.labels" . | nindent 4 }}
rules:
  # Watched resources from configuration
  {{- range $name, $resource := .Values.controller.config.watched_resources }}
  - apiGroups:
      {{- if eq $resource.api_version "v1" }}
      - ""
      {{- else }}
      - {{ $resource.api_version | regexFind "^[^/]+" }}
      {{- end }}
    resources:
      - {{ $resource.resources }}
    verbs: ["get", "list", "watch"]
  {{- end }}
  # Additional core resources
  - apiGroups: [""]
    resources: ["configmaps", "namespaces", "pods"]
    verbs: ["get", "list", "watch"]
{{- end }}
